First, the filename is the model name plus the method of data preprocessing

there are some hyperparameters you could adjust to your needs:
num_epochs
optimizer
criterion
dataset: if you change the dataset, num_classes should be changed respectively
